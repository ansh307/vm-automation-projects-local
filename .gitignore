# Vagrant-specific
.vagrant/

# Editor backup files
*~
*.swp
*.swo

# Log files (if any are generated in the root)
*.log

# Downloaded box files (if you build boxes locally and don't want to commit them)
# *.box


# Downloaded box files (if you build boxes locally and don't want to commit them)
# *.box

# Sensitive Information & Credentials
*.pem
*.key
*.ppk          # PuTTY private keys
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.secret
*.token
.env           # Environment variables, often containing secrets
secrets.yml
secrets.yaml
credentials.json
service-account-key.json
*passwords*.txt
*password*.txt
*access_keys*.csv
.truck